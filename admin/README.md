# Memories Admin

Memories 管理后台，基于 React、TypeScript 和 Vite 构建。

## 开发

```bash
npm install
npm run dev
```

开发服务器绑定到 `127.0.0.1`。生产构建输出到 `dist/`，依赖安装到 `node_modules/`；这两个目录都是本地生成内容，不需要提交。

## 构建

```bash
npm run build
npm run preview
```

`npm run build` 会先执行 TypeScript 检查，再生成静态资源。
